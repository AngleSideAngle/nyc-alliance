---
export interface Props {
  title: string;
}

const { title } = Astro.props;

import Layout from "./Layout.astro";
---

<Layout title={title}>
  <main class="prose max-w-[60rem] w-full mx-auto my-8 p-8">
    <slot />
  </main>
</Layout>
